# macOS
.DS_Store
._*

# Editors
.idea/
*.swp

# Node (only present if you run the mermaid/link tooling)
node_modules/

# Never commit credentials. The OAuth token and any GitHub token live in
# GitHub Secrets or on your own machine — see docs/05-security.md.
.env
.env.*
*.pem

# Session scaffolding — personal continuity notes, not part of the template
NEXT_SESSION_PROMPT.md

# Audit working papers from the 6–7 Sep sessions. Findings, not product.
# Kept because ~60 verified-but-unapplied findings live only here.
.audit/
